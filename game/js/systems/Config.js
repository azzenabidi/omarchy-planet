// Internal render resolution and world constants. The canvas scales to
// fit any monitor via Phaser.Scale.FIT; all layout math derives from
// these values instead of hardcoded pixels.
const GameConfig = {
    WIDTH: 1920,
    HEIGHT: 1080,
    TILE: 32
};
